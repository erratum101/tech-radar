# Пусто = запросы к /api с того же origin (рекомендуется в prod за reverse proxy).
# Для отдельного домена API: https://api.example.com
VITE_SHARE_API_BASE=

# API шаринга (см. server/index.js)
PORT=3001
# Для Docker/внешнего доступа без reverse proxy на том же хосте:
# HOST=0.0.0.0
